---
paths:
  - "**/*.import"
---

Before editing any .import file, consult `docs/reference/godot-import-settings.md` for correct setting names, valid values, and recommended overrides for low-poly asset packs.

Key overrides for low-poly tiles: `meshes/generate_lods=false`, `meshes/ensure_tangents=false`, `animation/import=false`.
